{
	"name": "lwangdu/ucnature-inat-observations",
	"description": "WordPress block plugin for displaying cached iNaturalist observations on UC Nature sites.",
	"type": "wordpress-plugin",
	"license": "GPL-2.0-or-later",
	"require-dev": {
		"wp-coding-standards/wpcs": "^3.2",
		"dealerdirect/phpcodesniffer-composer-installer": "^1.0"
	},
	"config": {
		"allow-plugins": {
			"dealerdirect/phpcodesniffer-composer-installer": true
		}
	},
	"scripts": {
		"lint:php": "find . -path './vendor' -prune -o -path './.git' -prune -o -name '*.php' -print -exec php -l {} \\;",
		"lint:js": "node --check assets/js/block.js && node --check assets/js/map.js && node --check assets/js/view.js",
		"phpcs": "phpcs --standard=WordPress-Core --extensions=php --ignore=vendor/* ."
	}
}
